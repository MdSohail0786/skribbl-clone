# Server
PORT=5000
NODE_ENV=development

# CORS - comma separated list of allowed frontend origins
CLIENT_ORIGIN=http://localhost:5173

# MongoDB Atlas connection string
# If left empty, the server falls back to an in-memory word bank
# and disables room history persistence (live gameplay still works fully).
MONGODB_URI=mongodb+srv://sohailsayed012336_db_user:sohail%40123@cluster0.lhyrdi8.mongodb.net/doodle-duel

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# Room defaults
DEFAULT_MAX_PLAYERS=8
DEFAULT_ROUNDS=3
DEFAULT_DRAW_TIME=80
